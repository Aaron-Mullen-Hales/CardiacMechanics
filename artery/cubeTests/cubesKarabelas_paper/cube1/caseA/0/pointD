FoamFile
{
    version 2.0;
    format ascii;
    class pointVectorField;
    object pointD;
}

dimensions [0 1 0 0 0 0 0];
internalField uniform (0 0 0);
boundaryField
{
    xMin { type calculated; }
    xMax { type calculated; }
    yMin { type calculated; }
    yMax { type calculated; }
    zMin { type calculated; }
    zMax { type calculated; }
}

