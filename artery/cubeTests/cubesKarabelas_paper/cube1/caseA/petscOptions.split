# Jacobian-free Newton-Krylov, matching the recipe already proven on
# ventricle/land/problem1: the hand-coded Jacobian (formJacobian) only
# includes the generic numerical stabilisation term, not the real
# constitutive tangent or D_x/D_y/D_z cross-coupling -- fine when a boundary
# DOF is Dirichlet-constrained, but produces a genuinely wrong Newton
# direction at a free (solidTraction) DOF whose equilibrium is actually
# governed by that missing tangent. -snes_mf/-snes_mf_operator makes the
# actual Newton step use a matrix-free (effectively exact) Jacobian action,
# using the hand-coded matrix only as a preconditioner.
#
# cube1 (direct fibre-axis loading) needs this combined with
# ../analytical/adaptive_continuation.py (verified per-step continuation,
# not a single Allrun pass) to reach beyond lambda~1.36 -- see README.md.
#
# SNES tolerance (-snes_atol/-snes_rtol) is the criterion that decides "is
# this genuinely converged" -- it is checked against a fresh evaluation of
# the true nonlinear residual F(x), not against the inner linear model, so
# it must stay tight (1e-6/1e-5) for an accepted result to be trustworthy.
# -ksp_rtol (the INNER linear solve, i.e. how precisely each Newton step
# direction is computed) is a different matter: standard inexact-Newton
# practice allows the inner linear solve to be loose, since SNES will keep
# taking more outer iterations regardless until the true residual (not the
# linear one) satisfies -snes_atol/-snes_rtol. Loosening ksp_rtol alone
# (while keeping snes_atol/rtol tight) was found empirically to help --
# unlike loosening snes_atol/rtol itself, this does not weaken what counts
# as "converged", only how each inner linear system is solved en route.
-snes_type newtonls
-snes_linesearch_type bt
-snes_atol 1e-6
-snes_rtol 1e-5
-snes_stol 1e-10
-snes_max_it 200
-snes_converged_reason

-snes_mf
-snes_mf_operator
-snes_max_linear_solve_fail 10000000
-snes_max_funcs 100000000

-ksp_type lgmres
-ksp_gmres_restart 200
-ksp_rtol 3e-3
-ksp_max_it 1000
-ksp_converged_reason
-pc_type fieldsplit
-pc_fieldsplit_block_size 4
-pc_fieldsplit_0_fields 0,1,2
-pc_fieldsplit_1_fields 3
-pc_fieldsplit_type schur
-pc_fieldsplit_schur_factorization_type lower
-pc_fieldsplit_schur_precondition a11
-fieldsplit_0_ksp_type preonly
-fieldsplit_0_pc_type hypre
-fieldsplit_0_pc_hypre_type boomeramg
-fieldsplit_1_ksp_type preonly
-fieldsplit_1_pc_type hypre
-fieldsplit_1_pc_hypre_type boomeramg

-snes_lag_preconditioner_persists true
-snes_lag_preconditioner -2
-snes_linesearch_alpha 1e-8
-snes_linesearch_minlambda 1e-12
